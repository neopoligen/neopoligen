#[cfg(test)]
mod page_children_tests {

    // use neopoligen_cli::page::Page;
    // use pretty_assertions::assert_eq;

    #[test]
    #[ignore] // need to make a new test file for this one
    pub fn page_children_test_basic() {
        // let page = Page::make_a_new_test_file_for_this_tktktkt();
        // let left = vec!["menu_foxtrot"];
        // let right = page.children();
        // assert_eq!(left, right);
    }
}
