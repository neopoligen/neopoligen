// This is not being used right now. the initial dev
// has a prorotype, but it's not being moved into prod yet

// use serde::Serialize;

// #[derive(Clone, Debug, PartialEq, Serialize)]
// #[serde(tag = "type", rename_all = "lowercase")]
// pub struct MenuItem {
//     pub page_id: String,
//     pub children: Vec<MenuItem>,
// }
