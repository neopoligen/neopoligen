[!- import "includes/theme-macros.neojinja" as theme -!]

[# NOTE: This section is really only for testing 
that the macros work. It's not expected to be used 
in regular output #]

<section 
[@ theme.section_attrs(site, page_id, section, ["macro_tests_section", "raw_section", "flow"]) -@]
>

<pre>

theme.link_or_title(site, page_id, "2fvdjqd5")
[@ theme.link_or_title(site, page_id, "2fvdjqd5") @]

theme.link_or_title(site, page_id, "2fyz3qjv")
[@ theme.link_or_title(site, page_id, "2fyz3qjv") @]

theme.link_or_title(site, page_id, "2fvdjqd5", { "title": "Alt Title" })
[@ theme.link_or_title(site, page_id, "2fvdjqd5", { "title": "Alt Title" }) @]

theme.link_or_title(site, page_id, "2fyz3qjv", { "title": "Alt Title" })
[@ theme.link_or_title(site, page_id, "2fyz3qjv", { "title": "Alt Title" }) @]

</pre>
