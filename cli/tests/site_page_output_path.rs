mod site_page_output_path {
    // use minijinja::Value;
    // use neopoligen::site::Site;
    // use pretty_assertions::assert_eq;

    // #[test]
    // pub fn path_for_page_without_override_path() {
    //     let site = Site::site1();
    //     let left =
    //         Some("leading-dir/Neopoligen/integration-site/docs/en/id_index/index.html".to_string());
    //     let right = site.page_output_path(&[Value::from("id_index")]);
    //     assert_eq!(left, right);
    // }
}
